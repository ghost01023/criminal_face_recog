install and add micromamba to system PATH

run: 

micromamba create -n ort \
  python=3.10 \
  cudatoolkit=11.8 \
  cudnn \
  onnxruntime=1.16.3 \
  numpy \
  opencv \
  -c conda-forge

micromamba install -c nvidia cuda-nvcc cuda-runtime cuda-libraries-dev


micromamba activate ort


python main.py



install rust and add binary location to system PATH
cd app
cargo install
